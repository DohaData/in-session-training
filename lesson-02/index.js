const favoriteBook = {
    bookName: 'The Art of War',
    author: 'Sun Tzu',
    haveRead: true,
};

favoriteBook;

test = [1, 2, 3];

test;

i = 2;
for (let t of test) {
    if ([2, 3].includes(t)){
        console.log(t);
    }
    else if (t === 1){
        console.log("happy now");
    }
}

test


console.log(`hello there, new stuff ${test}`)


let j = 8;

let k = j % 2 === 0;

console.log(k)
